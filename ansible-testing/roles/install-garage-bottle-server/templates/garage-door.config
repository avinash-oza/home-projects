[general]
port = {{ garage_door_port }}

[auth]
user={{ lookup('password', 'credentials/garage-door/garage-door-user.txt length=10') }}
password={{ lookup('password', 'credentials/garage-door/garage-door-password.txt length=10') }}
