[general]
port={{ microservice_port }}
# This is in F
critical_temperature=100.0
hostname={{ ansible_hostname}}
# mapping of sensor name to device id. Each should be comma seperated
sensor_names=GARAGE
sensor_ids=28-05167155beff

